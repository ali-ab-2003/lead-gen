# Core
python-dotenv>=1.0.1
pyyaml>=6.0.2
requests>=2.32.3

# Data acquisition
apify-client>=3.0.0

# NOTE: the optional Playwright fallback scraper (src/scrape_playwright.py) is
# NOT installed by default — it's heavy and unused by the Apify path. If you
# want it, install separately:  pip install "playwright>=1.48.0" && playwright install

# Storage (Supabase Postgres)
supabase>=2.9.0

# Google Sheets export
gspread>=6.1.4
google-auth>=2.35.0

# AI enrichment (Groq free tier)
groq>=0.13.0

# Dashboard
streamlit>=1.39.0
pandas>=2.2.3
